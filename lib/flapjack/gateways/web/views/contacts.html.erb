<% page_title 'Contacts' %>

<div class="page-header">
  <h2>Contacts</h2>
</div>

<div class="row">
  <div class="col-md-8">

    <form action="<%= @base_url %>contacts" method="get" role="form" class="form-horizontal">

      <div class="form-group">
        <label class="col-sm-2 control-label" for="start_time">Name:</label>
        <div class="col-sm-10">
          <input type="text" name="name" class="form-control" size="20" maxlength="80" value="<%= h params[:name] %>">
          <span class="help-block">
            e.g. the exact name, or a /regexp/
          </span>
        </div>

        <div class="form-group">
          <div class="col-sm-2"></div>
          <div class="col-sm-10">
            <button type="submit" class="btn btn-success">Refresh</button>
          </div>
        </div>

      </div>

    </form><!-- form-horizontal-->

  </div>
</div>

<% if !@contacts || @contacts.empty? %>
  <p>No contacts</p>
<% else %>
  <table class="table table-bordered table-hover table-condensed">
    <tr>
      <th>Name</th>
    </tr>
    <% @contacts.each do |contact| %>
    <tr>
      <td><a href="<%= @base_url %>contacts/<%= contact[:id] %>" title="contact details"><%= h contact[:name] %></a></td>
    </tr>
    <% end %>
  </table>
<% end %>
<br>

<%= erb '_pagination.html'.to_sym, :layout => false %>
