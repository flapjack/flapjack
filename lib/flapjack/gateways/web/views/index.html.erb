<% page_title 'Summary' %>

<div class="page-header">
  <h2>Summary</h2>
</div> <!-- page header -->

<h4><a href="<% @base_url %>checks?type=failing" title="failing checks"><%= h @count_failing_checks %></a> out of <a href="<% @base_url %>checks" title="all checks"><%= h @count_all_checks %></a> checks are failing</h4>
